apiVersion: v1
kind: Secret
metadata:
  name: vortexflow-secrets
  namespace: vortexflow
type: Opaque
stringData:
  # PRODUCTION: Replace with actual base64-encoded values or use External Secrets Operator
  Jwt__Secret: "CHANGE_ME_USE_A_STRONG_SECRET_AT_LEAST_32_CHARS"
  ApiKeys__Internal: "VORTEX_INTERNAL_DEV_KEY_999"
  POSTGRES_PASSWORD: "vortexflow_pass"
  REDIS_PASSWORD: ""
  RABBITMQ_DEFAULT_USER: "guest"
  RABBITMQ_DEFAULT_PASS: "guest"
